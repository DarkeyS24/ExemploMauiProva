<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MobileApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MobileApp"
    xmlns:views="clr-namespace:MobileApp.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="MobileApp">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="Login" />

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="Calendário"
        ContentTemplate="{DataTemplate views:CalendarioAtendimentosPage}"
        Route="CalendarioAtendimentos" />

    <ShellContent
        Title="Meus Atendimentos"
        ContentTemplate="{DataTemplate views:MeusAtendimentosPage}"
        Route="MeusAtendimentos" />

</Shell>
